# 部署说明

## 最简单方式

把压缩包解压后，将所有文件上传到 GitHub Pages 仓库根目录。GitHub Pages 必须能在根目录看到 `index.html`。

## 自定义域名

当前 `CNAME` 为：

```text
1956799.xyz
```

如果你仍使用这个域名，不需要修改该文件。

## 注意

你当前线上仓库的 `index.html` 是随机挑选器，而旧个人主页在 `index-index.html`。部署这个打包版时，新 `index.html` 会成为真正的个人主页入口。

建议部署前保留旧文件副本或单独建分支。
